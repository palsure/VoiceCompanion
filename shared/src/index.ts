/**
 * Shared module exports for VoiceCompanion
 * This module can be used by both mobile (React Native) and web (React) apps
 */

export * from './types'
export * from './api'
export { createApiClient, createApiServices, VoiceCompanionApi } from './api'

